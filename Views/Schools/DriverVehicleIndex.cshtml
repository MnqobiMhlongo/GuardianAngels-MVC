@model IEnumerable<GuardianAngels.Models.Vehicle>

@{
    ViewBag.Title = "DriverVehicleIndex";
    Layout = "~/Views/Shared/newLayout1.cshtml";
}

<br />
<center>
    <h2>Select Your Vehicle</h2>
</center>


@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.School.SchoolName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Plate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Color)
            </th>

            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.School.SchoolName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Plate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Color)
            </td>

            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.VehicleId }) |
                @Html.ActionLink("Details", "Details", new { id=item.VehicleId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.VehicleId })
            </td>
        </tr>
    }

    </table>
*@


<br />
<div class="row">
    @foreach (var item in Model)
    {
        <div style="padding-left:15px">
            <div class="card mb-5" style="padding-left:30px">
                <a href="@Url.Action("busKids", new { id = item.SchoolId })" class="d-flex align-items-center">
                    <img src="~/images/school-bus.gif" alt="Student Image" class="img-fluid" style="width: 100px; height: 100px; object-fit: cover; margin-right: 20px;">
                    <div class="card-body">
                        <h5 class="card-title">@item.Name</h5>
                        <h5 class="card-title">@item.Color</h5>
                        <h5 class="card-title">@item.Plate</h5>
                    </div>
                </a>
            </div>
        </div>

    }
</div>